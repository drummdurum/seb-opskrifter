<!DOCTYPE html>
<html lang="da">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Opskrifter</title>
  <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-sand-light min-h-screen">
  <nav class="bg-warm-beige shadow-md mb-8 border-b-4 border-red-accent">
    <div class="container mx-auto px-4 py-4">
      <div class="flex flex-col sm:flex-row justify-between items-center gap-4">
        <a href="/" class="text-xl sm:text-2xl font-bold text-dark-brown">🍳 Opskrifter</a>
        <div class="flex flex-wrap gap-2 sm:gap-4 justify-center">
          <a href="/" class="text-dark-brown hover:text-red-accent transition-colors duration-200 text-sm sm:text-base">Alle opskrifter</a>
          <a href="/tags" class="text-dark-brown hover:text-red-accent transition-colors duration-200 text-sm sm:text-base">Tags</a>
          <a href="/ny" class="btn-primary text-sm sm:text-base">+ Ny opskrift</a>
        </div>
      </div>
    </div>
  </nav>

  <main class="container mx-auto px-4 pb-12">
    <div class="max-w-3xl mx-auto">
      <h1 class="text-2xl sm:text-3xl font-bold text-dark-brown mb-4 sm:mb-6"><%= title %></h1>

      <div class="bg-white rounded-lg shadow-md p-4 sm:p-8">
        <% if (tags.length === 0) { %>
          <p class="text-tree-brown text-center text-sm sm:text-base">Ingen tags fundet endnu. Tilføj tags til dine opskrifter!</p>
        <% } else { %>
          <div class="flex flex-wrap gap-2 sm:gap-3">
            <% tags.forEach(tag => { %>
              <a href="/tag/<%= tag %>" class="tag text-lg">
                <%= tag %>
              </a>
            <% }) %>
          </div>
        <% } %>
      </div>
    </div>
  </main>

  <footer class="bg-dark-brown text-sand-light text-center py-4 mt-12 border-t-4 border-red-accent">
    <p class="text-sm sm:text-base">&copy; <%= new Date().getFullYear() %> Opskrifter Database</p>
  </footer>

  <script src="/js/main.js"></script>
</body>
</html>
